{
  "name": "lectura-oral-vercel",
  "version": "1.0.0",
  "description": "Monorepo (frontend + backend) de evaluación de lectura oral infantil, listo para desplegar en Vercel.",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "build": "npm run build:backend && npm run build:frontend && node scripts/collect-output.mjs",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.24",
    "typescript": "^5.5.3"
  }
}
